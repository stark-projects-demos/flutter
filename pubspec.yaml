name: teamsync_demo
description: A minimal Flutter app demonstrating TeamSync landing page for accessibility scanning
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  custom_lint: ^0.8.1
  stark_accessibility_lint: ^1.0.1

flutter:
  uses-material-design: true
